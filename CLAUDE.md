# CLAUDE.md

## Project context

This is **not** the upstream `MiniCodeMonkey/chief` repo and it is **not** a
contribution fork whose changes get proposed back upstream. It is a small,
**private improvement project** that Ben uses for himself.

`origin` points at Ben's private repo (`ben182/chief`). Committing and pushing
directly to `main` here is fine and expected — no PR-to-upstream workflow, no
feature-branch ceremony unless Ben asks for it.
